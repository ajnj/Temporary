<?php
/**
 * Helper functions for migrate_helios.
 */

/**
 * Get the main subvalue
 */
/*
function migrate_helios_get_main_subvalue($value) {
  // Get the main value of a field, thus anything before [[...]]
  $exploded = explode('[[', $value);
  if (is_array($exploded)) {
    $value = trim($exploded[0]);
    $value = migrate_helios_replace_angle_brackets($value);
  }
  return $value;
}
*/
